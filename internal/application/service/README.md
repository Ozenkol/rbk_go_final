# Сервисы приложения

Этот пакет содержит логику сервисов приложения.

## AuthService
`AuthService` управляет аутентификацией пользователей, генерацией и валидацией JWT токенов. Токены теперь включают `company_id` для обеспечения связи пользователя с конкретной компанией.
